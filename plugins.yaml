# OpenLearning 插件配置
#
# 每个插件可以配置:
#   enabled: true/false — 是否启用
#   (其他自定义配置项)

plugins:
  example-rss:
    enabled: false  # 示例插件默认禁用
    # feed_url: "https://hnrss.org/newest"

  # 自定义插件示例:
  # my-custom-source:
  #   enabled: true
  #   api_key: "your-api-key"
  #   base_url: "https://api.example.com"
